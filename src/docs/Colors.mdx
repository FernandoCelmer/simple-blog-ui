import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';

<Meta title="Design/Colors" />

# Colors

Tokens from `src/styles/root.css` — the same palette used by `mkdocs-simple-blog`.

<ColorPalette>
  <ColorItem
    title="Core"
    subtitle="Black / white / gray / primary surfaces"
    colors={{
      text: '#000000',
      title: '#000000',
      primary: '#000000',
      background: '#ffffff',
      'color-gray': '#808080',
    }}
  />
  <ColorItem
    title="Accents"
    subtitle="Used for state and semantic emphasis"
    colors={{
      'color-blue': '#4051b5',
      'color-green': '#4cae4f',
      'color-yellow': '#f1dc15',
      'color-orange': '#ffa724',
      'color-purple': '#ab47bd',
      'color-red': '#ff2c06',
    }}
  />
</ColorPalette>

## Using the tokens

```css
.my-element {
  color: var(--text);
  background-color: var(--background);
  border: 2px solid var(--primary);
}

.alert--error {
  border-color: var(--color-red);
  color: var(--color-red);
}
```
