import { Meta, Primary, Controls, Stories } from '@storybook/blocks';
import { FrameworkTabs } from '../../../../.storybook/FrameworkTabs.jsx';
import * as S from './sb-button-group.stories.js';

<Meta of={S} />

# Button Group

Container that joins multiple buttons with shared borders. `horizontal` (default) or `vertical`.

## Usage

<FrameworkTabs
  html={`<sb-button-group label="Align">
  <sb-button variant="secondary">Left</sb-button>
  <sb-button variant="secondary">Center</sb-button>
  <sb-button variant="secondary">Right</sb-button>
</sb-button-group>`}
  react={`import { ButtonGroup, Button } from 'simple-blog-ui/react';

<ButtonGroup label="Align">
  <Button variant="secondary">Left</Button>
  <Button variant="secondary">Center</Button>
  <Button variant="secondary">Right</Button>
</ButtonGroup>`}
  vue={`<sb-button-group label="Align">
  <sb-button variant="secondary">Left</sb-button>
  <sb-button variant="secondary">Center</sb-button>
  <sb-button variant="secondary">Right</sb-button>
</sb-button-group>`}
/>

## Primary

<Primary />

## Controls

<Controls />

## Stories

<Stories />
