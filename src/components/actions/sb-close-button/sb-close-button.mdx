import { Meta, Primary, Controls, Stories } from '@storybook/blocks';
import { FrameworkTabs } from '../../../../.storybook/FrameworkTabs.jsx';
import * as S from './sb-close-button.stories.js';

<Meta of={S} />

# Close Button

Simple dismissal button with `×` glyph. Use the `inverse` prop over dark backgrounds. Emits `sb-close`.

## Usage

<FrameworkTabs
  html={`<sb-close-button label="Close dialog"></sb-close-button>`}
  react={`import { CloseButton } from 'simple-blog-ui/react';

<CloseButton label="Close dialog" onClose={() => {}} />`}
  vue={`<sb-close-button label="Close dialog" @sb-close="fn"></sb-close-button>`}
/>

## Primary

<Primary />

## Controls

<Controls />

## Stories

<Stories />
