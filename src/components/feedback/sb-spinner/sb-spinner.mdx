import { Meta, Primary, Controls, Stories } from '@storybook/blocks';
import { FrameworkTabs } from '../../../../.storybook/FrameworkTabs.jsx';
import * as S from './sb-spinner.stories.js';

<Meta of={S} />

# Spinner

Circular loading indicator with three sizes (`sm`, `md`, `lg`). Pass `label` for accessibility.

## Usage

<FrameworkTabs
  html={`<sb-spinner size="md" label="Loading"></sb-spinner>`}
  react={`import { Spinner } from 'simple-blog-ui/react';

<Spinner size="md" label="Loading" />`}
  vue={`<sb-spinner size="md" label="Loading"></sb-spinner>`}
/>

## Primary

<Primary />

## Controls

<Controls />

## Stories

<Stories />
