import { Meta, Primary, Controls, Stories } from '@storybook/blocks';
import { FrameworkTabs } from '../../../../.storybook/FrameworkTabs.jsx';
import * as S from './sb-code.stories.js';

<Meta of={S} />

# Code

Inline chip or block code. Block mode accepts a `language` prop for syntax highlighting via highlight.js; omit to auto-detect.

## Usage

<FrameworkTabs
  html={`<sb-code>npm install</sb-code>

<sb-code block language="javascript">
const add = (a, b) => a + b;
</sb-code>`}
  react={`import { Code } from 'simple-blog-ui/react';

<Code>npm install</Code>

<Code block language="javascript">{\`const add = (a, b) => a + b;\`}</Code>`}
  vue={`<sb-code>npm install</sb-code>

<sb-code block language="javascript">
  const add = (a, b) => a + b;
</sb-code>`}
/>

## Primary

<Primary />

## Controls

<Controls />

## Stories

<Stories />
